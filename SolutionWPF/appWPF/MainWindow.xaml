<Window x:Class="appWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:appWPF" 
         xmlns:vms="clr-namespace:appWPF.ViewModels" 
        xmlns:views="clr-namespace:appWPF.Views" 
        xmlns:custom="clr-namespace:ModalControl;assembly=ModalControl"
        mc:Ignorable="d"
        FontSize="14"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vms:CreateSaveViewModel}">
            <views:CreateSaveView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vms:EditSaveViewModel}">
            <views:EditSaveView/>
        </DataTemplate>
    </Window.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <custom:Modal Panel.ZIndex="1" IsOpen="{Binding IsModalOpen}">
                <ContentControl Margin="50 25" Content="{Binding CurrentModalViewModel}"/>
            </custom:Modal>
            
            <views:UserView DataContext="{Binding SavesViewModel}"/>
        </Grid>
    </ScrollViewer>
</Window>
